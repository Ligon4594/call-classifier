{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python run.py --days 7 --send-email",
    "restartPolicyType": "NEVER",
    "cronSchedule": "0 12 * * 1"
  }
}
